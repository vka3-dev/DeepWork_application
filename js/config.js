(() => {
    "use strict";

    // Replace this value with the deployed API origin during the deployment phase.
    // Keep the value origin-only: do not add a trailing slash or an API path.
    window.DeepWorkConfig = Object.freeze({
        API_BASE_URL: "https://deepwork-api-7yek.onrender.com"
    });
})();
